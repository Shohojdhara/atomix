/**
 * Utility Functions Exports
 */

export {
  mergeClassNames,
  applyPartStyles,
  createCSSVarStyle,
  mergeComponentProps,
  getPartStyles,
  createPartProps,
  hasCustomization,
  createDebugAttrs,
  generateUUID,
  isYouTubeUrl,
  extractYouTubeId,
} from './componentUtils';

export type {
  MergePropsOptions,
} from './componentUtils';
