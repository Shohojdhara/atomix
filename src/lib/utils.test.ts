import { describe, it, expect } from 'vitest';

// Example test - replace with actual utility functions from your lib
describe('Utils', () => {
  it('should be able to run tests', () => {
    expect(true).toBe(true);
  });

  // Add more tests for your actual utility functions
  // Example:
  // it('should format className correctly', () => {
  //   expect(formatClassName('test', 'class')).toBe('test class');
  // });
});
